<%= form_for(poo) do |f| %>
  <% if poo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(poo.errors.count, "error") %> prohibited this poo from being saved:</h2>

      <ul>
      <% poo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
